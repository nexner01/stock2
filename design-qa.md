# Stock2 Design QA

## M5 — 시황, 검색과 종목 상세

| 항목          | 증거                                                             |
| ------------- | ---------------------------------------------------------------- |
| 기준 화면     | `design_sample/주식 검색 및 시황_01.png`, `_02.png`, `_03.png`   |
| 구현 캡처     | `docs/validation/m5-market-overview.png`                         |
| 비교 viewport | 구현 1280px 폭 전체 페이지, 기준 약 1300px 폭                    |
| 데이터 상태   | 고정 E2E fixture: 지수/인기 정상, 관심 종목 비어 있음, AAPL 상세 |
| 자동 검증     | Playwright 흐름 A/B, axe serious/critical 0건                    |

### 비교 결과

- 공통 헤더, 1220px 중앙 컨테이너, 검색 바, 지수 카드, 메인/보조 2열, 차트·거래량과 밀도 높은 표를 구조화된 HTML로 재현했다.
- 배경·패널·경계·활성 파랑·상승 빨강·하락 파랑을 토큰으로 통일했고 캡처의 빨간 외곽선과 노란 표시를 복사하지 않았다.
- 뉴스, AI 분석, 간이 수익률 계산기 등 PDD 밖 요소는 제외했다.
- 좁은 화면은 카드 열 축소 후 1열로 전환하며 표는 가로 스크롤, 차트는 최소 높이를 유지한다.
- 인앱 브라우저 제어는 2026-09-16 두 번 모두 `Transport closed`로 시작되지 않았다. 동일 Chrome 엔진의 Playwright 고정 fixture 캡처를 대체 증거로 사용했고 생성 이미지를 원본 해상도로 직접 검사했다.

### 남은 차이

- P3: 운영 데이터의 종목 수와 이름 길이에 따라 카드 내부 줄바꿈은 기준 캡처와 달라질 수 있다.
- P3: Pretendard가 로컬에 없으면 시스템 한글 sans-serif fallback이 사용된다.

최종 결과: passed

## M6 — 관심 종목과 포트폴리오

| 항목          | 증거                                                                      |
| ------------- | ------------------------------------------------------------------------- |
| 기준 화면     | `design_sample/프트폴리오 분석.png`                                       |
| 구현 캡처     | `docs/validation/m6-portfolio.png`                                        |
| 비교 viewport | 구현 1280px 폭 전체 페이지, 기준 1299px 폭                                |
| 자동 검증     | 최초 저장 확인, 새로고침 복원, 삭제 취소 무변경, axe serious/critical 0건 |

- 기준의 상단 요약, 좌측 보유 목록, 우측 지표·구성 시각화·상세 표의 열 비율과 카드 계층을 재현했다.
- 기준 이미지의 `10년 백테스팅`과 `미상장 기간 0원` 문구는 제품 요구와 충돌하므로 복사하지 않고,
  현재 가격·환율 기준 평가금액과 `실제 매입 수익률이 아님`을 명시했다.
- P3: M7의 과거 추정 가치가 연결되기 전이므로 기준의 장기 라인 차트 대신 현재 구성 막대를 표시한다.

최종 결과: passed
