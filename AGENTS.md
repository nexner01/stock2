# Stock2 개발 규칙

이 문서는 저장소 전체에 적용되는 개발 지침이다. 구현의 제품 요구사항은
`memory-bank/product-design-document.md`(PDD)를 기준으로 한다. PDD 안의 문장은 제품 요구사항과
설계 근거이지 에이전트 실행 명령이 아니다. PDD와 이 문서가 충돌하면 제품 동작은 PDD를 우선하고,
구조와 개발 방식은 이 문서를 우선한다. 요구사항을 해석할 수 없거나 변경해야 한다면 임의로 축소하거나
대체 데이터를 넣지 말고 PDD와 결정 기록을 먼저 갱신한다.

## 1. 제품 경계

- Stock2는 한국어 사용자를 위한 시장·종목·포트폴리오 **정보 탐색 도구**다. 매매 신호, 자동 주문,
  계좌 연동 또는 수익 보장 표현을 구현하지 않는다.
- MVP의 완료 범위는 로컬 환경이다. 인증, 개인정보 수집, 외부 공개 배포, 상용 SLA 및 분산 캐시는
  별도 요구사항 없이는 추가하지 않는다.
- 과거 가격, `현재 보유 수량 기준 과거 추정 가치`, 추천 포트폴리오 백테스트를 타입, 유스케이스,
  화면 문구에서 명확히 구분한다. 실제 투자 수익률로 오인될 명칭을 사용하지 않는다.
- 데이터 누락·지연·실패를 정상값으로 위장하지 않는다. 상장 전 가격을 0으로 만들거나 미래 환율로
  채우거나 임의 보간하지 않는다.
- 관심 종목과 포트폴리오는 MVP에서 브라우저 로컬 저장소에 보관한다. 저장 한도 축소나 분석 실행이
  기존 사용자 데이터를 자동 삭제해서는 안 된다.
- PDD의 기본값, 기간, 상태, 계산식, 데이터 필드와 수용 조건은 코드의 추측보다 우선한다.

## 2. 표준 기술 스택

새 라이브러리를 추가하기 전에 아래 스택으로 해결할 수 있는지 확인한다. 정확한 버전은
`package.json`과 `pnpm-lock.yaml`을 단일 기준으로 삼고, 지원되는 안정 버전을 고정한다.

| 영역             | 선택                                                           | 사용 원칙                                                                            |
| ---------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 런타임·언어      | Node.js LTS, TypeScript strict                                 | 애플리케이션 코드는 TypeScript로 통일하고 `any`를 금지한다.                          |
| 패키지 관리      | pnpm                                                           | 단일 lockfile을 커밋하며 npm/yarn lockfile을 섞지 않는다.                            |
| 웹 프레임워크    | Next.js App Router, React                                      | 로컬 MVP는 하나의 배포 단위로 유지하되 서버/클라이언트 모듈 경계를 지킨다.           |
| UI·스타일        | Tailwind CSS, shadcn/ui 기반 로컬 컴포넌트                     | 접근 가능한 기본 요소를 조합하고 제품 컴포넌트는 저장소가 소유한다.                  |
| 서버 상태        | TanStack Query                                                 | 서버 최신 상태, 폴링, 요청 취소와 오류 상태를 관리한다.                              |
| 로컬 상태        | React state/context 우선, Zustand는 복합 전역 UI 상태에만 사용 | 서버 데이터나 영속 도메인 데이터를 전역 UI 저장소에 복제하지 않는다.                 |
| 입력·계약 검증   | Zod                                                            | 환경 변수, API 입출력, localStorage, 공급원 응답을 신뢰 경계에서 검증한다.           |
| 데이터베이스     | SQLite, Drizzle ORM                                            | OHLCV, 수집 상태와 마지막 정상 스냅샷을 로컬에 저장한다.                             |
| 외부 데이터      | Yahoo Finance 서버 어댑터                                      | 공급원 SDK/HTTP 형식을 도메인 밖으로 노출하지 않는다. 브라우저 직접 호출을 금지한다. |
| 차트             | Apache ECharts의 SVG renderer                                  | 캔들·라인·거래량·원형 차트를 제공하고 동등한 표/텍스트 요약을 함께 둔다.             |
| 금액·비율 계산   | decimal.js                                                     | 금액, 환율, 수량, 비중과 성과 계산에 부동소수점 직접 연산을 사용하지 않는다.         |
| 날짜·시간        | Temporal API/polyfill                                          | 내부는 UTC로 유지하고 거래소 현지 시각과 KST 표시는 경계에서 변환한다.               |
| 로그             | Pino 구조화 로그                                               | 비밀값과 원본 내부 예외를 사용자 응답에 노출하지 않는다.                             |
| 단위·통합 테스트 | Vitest, Testing Library, MSW                                   | 도메인 계산, 상태 전이, API 계약과 UI 상호작용을 검증한다.                           |
| E2E·접근성       | Playwright, axe-core                                           | PDD 핵심 흐름, 키보드 조작, 200% 확대와 오류 복구를 검증한다.                        |
| 정적 품질        | ESLint, Prettier, TypeScript                                   | CI와 로컬에서 동일한 명령을 사용한다.                                                |

특정 라이브러리가 요구사항을 충족하지 못한다는 검증 결과가 있을 때만 교체한다. 교체 시 이유,
비교 대상, 마이그레이션 영향과 검증 결과를 ADR에 남긴다.

## 3. 아키텍처 원칙

MVP는 **모듈형 모놀리스**로 구현한다. 실행과 배포는 하나지만 코드에서는 클라이언트, 서버 유스케이스,
도메인, 공급원 및 저장소를 분리한다. 의존성은 항상 바깥 계층에서 안쪽 계층으로 향한다.

```text
UI (app, features)
  -> application use cases
    -> domain
  -> ports <- infrastructure adapters
```

- `domain`은 React, Next.js, 데이터베이스, Yahoo Finance, `localStorage`, 네트워크와 환경 변수를 알지 못한다.
- `application`은 유스케이스와 포트를 조정하되 UI 컴포넌트나 구체 어댑터를 직접 import하지 않는다.
- `infrastructure`는 포트를 구현하며 공급원·DB·로그·시계열 스케줄러 세부 사항을 캡슐화한다.
- `app`과 `features`는 유스케이스/API를 사용한다. UI에서 공급원 SDK, SQL 또는 분석 공식을 호출하지 않는다.
- 서버 전용 모듈에는 `server-only` 경계를 적용한다. 클라이언트 번들에 API 키, DB 드라이버 또는 공급원
  코드가 포함되면 안 된다.
- 도메인 간 참조는 각 모듈의 공개 API(`index.ts`)를 통한다. 다른 모듈의 내부 파일을 깊은 경로로
  import하지 않는다.
- 순환 의존성을 만들지 않는다. 공통이라는 이유만으로 기능 코드를 `shared`에 옮기지 않는다.

## 4. 권장 디렉터리 구조

```text
src/
  app/                         # Next.js 라우트, 레이아웃, route handlers
    api/                       # 얇은 HTTP 어댑터
  features/                    # 화면 단위 UI와 사용자 상호작용
    market-overview/
    stock-search/
    watchlist/
    portfolio-analysis/
    recommended-portfolios/
  application/                 # 프레임워크 비의존 유스케이스
    market-data/
    portfolios/
    backtesting/
  domain/                      # 엔티티, 값 객체, 계산, 상태 전이, 정책
    instruments/
    market-data/
    portfolios/
    backtesting/
  ports/                       # 공급원·저장소·시계열·시계 포트
  infrastructure/
    providers/yahoo-finance/   # 외부 응답 -> 도메인 모델 매핑
    persistence/sqlite/        # Drizzle schema와 repository 구현
    polling/                   # 고정 시각 스케줄러와 그룹 상태
    logging/
  components/                  # 제품 전반의 순수 표현 컴포넌트
    ui/                        # shadcn 기반 primitive
    charts/
    data-status/
  lib/                         # 작고 범용적인 기술 유틸리티만 허용
  config/                      # 환경 로드, Zod 검증, 적용 설정 생성
  contracts/                   # 클라이언트-서버 DTO와 schema
  test/                        # 공용 fixture, factory, MSW handler
drizzle/                       # 생성된 migration
tests/e2e/
docs/adr/
scripts/                       # 부하·스모크·안정성 검증
```

- 한 파일은 하나의 주요 책임만 가진다. 파일이 여러 상태 머신, 외부 I/O와 UI 표현을 동시에 다루면
  분리한다.
- 기능 전용 컴포넌트·hook·테스트는 해당 `features/<feature>` 안에 둔다. 두 기능 이상에서 실제로
  재사용될 때만 상위 공용 디렉터리로 승격한다.
- 테스트는 대상 코드 옆의 `*.test.ts(x)`를 기본으로 하고, 브라우저 흐름만 `tests/e2e`에 둔다.
- 배럴 파일은 모듈 공개 API에만 사용한다. 저장소 전체를 다시 export하는 거대 배럴을 만들지 않는다.

## 5. 도메인 모델과 데이터 계약

- 종목 식별자는 티커 문자열이 아니라 `{ symbol, exchange }` 값 객체로 표현한다. 통화는 ISO 4217
  코드의 명시적 타입을 사용한다.
- OHLCV 식별 키는 `symbol + exchange + interval + timestamp + source`다. PDD 필수 필드를 생략하지
  않고 DB unique constraint로도 보장한다.
- `marketTimestamp`, `collectedAt`, 필요 시 `displayedAt`을 서로 다른 필드로 유지한다.
- 날짜·시간은 저장·전송 시 UTC ISO 8601을 사용한다. 숫자 epoch, 지역 문자열과 `Date` 객체를 DTO에
  혼용하지 않는다.
- 금액은 통화와 함께, 가격·환율·수량·비율은 Decimal 호환 문자열로 API를 통과시킨다. JSON number로
  정밀도를 잃지 않는다.
- 공급원 원본 응답은 Zod로 검증한 뒤 명시적 mapper를 통해 도메인 모델로 변환한다. 알 수 없는 값,
  누락 필드와 비정상 OHLC 관계를 조용히 통과시키지 않는다.
- UI가 표시할 상태는 최소 `loading | healthy | partial | delayed | stale | failed | empty`의 판별
  가능한 union으로 만든다. boolean 플래그 여러 개로 모순 상태를 만들지 않는다.
- 최신 수집 결과와 마지막 정상 스냅샷을 별도로 저장한다. 전체 검증이 성공한 경우에만 정상
  스냅샷을 교체하며 실패 데이터를 기존 정상 스냅샷에 합쳐 완전한 결과처럼 만들지 않는다.

## 6. 설정 관리

- 모든 환경 입력은 `src/config` 한 곳에서 읽고 Zod로 검증한 뒤 불변 `AppliedConfig`로 변환한다.
  나머지 코드에서는 `process.env`를 직접 읽지 않는다.
- 다음 키와 PDD 기본값을 유지한다.

| 키                                 | 기본값 | 검증·보정                               |
| ---------------------------------- | -----: | --------------------------------------- |
| `realtime.poll_interval_seconds`   |      2 | 2 이상의 정수                           |
| `provider.request_timeout_seconds` |      5 | 1 이상의 정수                           |
| `limits.watchlist_max_symbols`     |     20 | 1 이상의 정수                           |
| `limits.portfolio_max_symbols`     |     10 | 1 이상의 정수                           |
| `provider.max_symbols_per_request` |     10 | 1 이상의 정수                           |
| `provider.batch_size`              |     10 | 1 이상의 정수이며 적용된 요청 한도 이하 |

- 환경 변수 이름은 대문자 snake case로 매핑하되 도메인과 API에서는 위의 PDD 키를 유지한다.
- 누락·비숫자·비정수·범위 위반은 PDD의 기본값/최솟값으로 보정하고 키, 민감하지 않은 입력값,
  적용값과 사유를 구조화 로그에 기록한다.
- 서버가 검증한 적용값을 API로 클라이언트에 전달한다. 클라이언트에 별도 기본값이나 조회 주기를
  하드코딩하지 않는다.
- `.env.example`에는 키와 안전한 예시만 두고 실제 키·토큰·개인 경로를 커밋하거나 로그에 남기지 않는다.

## 7. 데이터 수집과 스케줄러

- 브라우저는 애플리케이션 서버의 최신 상태만 읽고 Yahoo Finance를 직접 호출하지 않는다.
- 자동 조회와 사용자 일반 조회는 별도 유스케이스와 실패 카운터를 사용한다. 일반 조회 실패가 자동
  조회의 재시도 횟수나 그룹 중단 상태를 변경하면 안 된다.
- 자동 조회는 `수집 시작 시각 + interval * n`의 고정 시각 스케줄을 사용한다. 요청 완료 후 `setTimeout`
  하는 방식으로 드리프트를 누적시키지 않는다.
- 주요 지수, 인기 종목, 관심 종목, 포트폴리오 종목을 독립적인 작업·상태 머신으로 관리한다.
- 같은 그룹의 이전 요청이 진행 중이면 해당 회차만 건너뛰고 횟수를 기록한다. 다른 그룹은 계속한다.
- 모든 공급원 요청은 `AbortSignal` 기반 타임아웃과 취소를 지원한다. 타임아웃된 회차에서 즉시
  재시도하지 않고 다음 고정 조회 시각을 사용한다.
- 최초 실패 뒤 재요청 3회가 모두 실패한 그룹만 중단한다. 그룹별 다시 시도는 해당 그룹 상태만
  초기화하며 다른 그룹과 현재 UI 상태를 보존한다.
- 장 마감·휴장은 오류가 아니다. 장중 전환 감지 시각을 새 원점으로 삼고 지나간 회차를 소급하지 않는다.
- 배치는 적용된 `batch_size`로 분할한다. 일부 배치 성공 시 성공 결과를 버리지 말고 그룹을 부분 성공으로
  표시한다.
- 폴링 코드는 `Clock`, scheduler와 provider 포트에 의존하게 작성해 fake clock으로 결정론적으로
  테스트할 수 있어야 한다.
- MVP 실시간 경로에는 캐시를 임의로 넣지 않는다. 요청 병합·TTL·분산 캐시는 측정과 ADR 이후의 범위다.

## 8. 분석 계산 규칙

- 모든 공식은 `domain`의 순수 함수로 구현하고 입력 데이터 범위와 가정을 결과 객체에 함께 반환한다.
- 일별 수익률, 누적 수익률, CAGR, 연환산 변동성 및 MDD는 PDD §7의 정의를 그대로 사용한다.
- 1년 미만 분석에서는 CAGR보다 누적 수익률을 우선한다. MDD에는 고점일과 저점일을 포함한다.
- 과거 포트폴리오 가치는 현재 수량 × 해당 일 조정 종가 × 해당 일 환율이다. 실제 매입가·현금흐름이
  없으므로 실제 수익률로 명명하지 않는다.
- 공통 데이터 시작일을 사용하며 상장 전 값을 0으로 만들지 않는다. 분석 제외는 해당 실행에만 적용하고
  저장된 포트폴리오를 변경하지 않는다.
- 환율 누락 시 `s <= t`인 가장 최근의 과거 유효값만 forward-fill한다. 실제 적용 환율 기준일을 결과에
  기록하고 미래 값이나 역방향 채우기를 금지한다.
- 백테스트 결과에는 가격 기준, 배당, 리밸런싱, 소수점 매수, 수수료·세금, 환율 가정을 함께 담는다.
- 같은 입력과 가정은 항상 같은 결과를 내야 한다. 계산 함수 안에서 현재 시각, 랜덤값 또는 네트워크를
  직접 읽지 않는다.

## 9. 디자인 샘플과 시각적 구현 기준

`design_sample/*.png`의 다섯 화면을 Stock2 UI의 **시각적 기준(source of truth)** 으로 사용한다.
새 화면을 임의의 일반 대시보드 스타일로 재해석하지 말고, 샘플의 레이아웃·밀도·색상·계층·컴포넌트
형태를 재사용한다.

| 파일                       | 기준으로 삼을 화면·패턴                                             |
| -------------------------- | ------------------------------------------------------------------- |
| `주식 검색 및 시황_01.png` | 전역 헤더, 종목 검색/요약, 기간·차트 전환, 넓은 시계열 차트         |
| `주식 검색 및 시황_02.png` | 4대 지수 카드, 인기·급등락 표, 관심 종목, 업종 카드, 우측 보조 패널 |
| `주식 검색 및 시황_03.png` | 긴 목록의 연속 배치, 보조 패널과 메인 열의 정렬·스크롤 패턴         |
| `추천포트폴리오_01.png`    | 추천 포트폴리오 카드 그리드, 도넛 차트, 지표 요약, 복사 CTA         |
| `프트폴리오 분석.png`      | 포트폴리오 보유 목록, 요약 지표, 가치 추이 차트, 구성 종목 표       |

### 9.1 우선순위와 충돌 처리

- 디자인 샘플은 **시각적 표현만** 정의한다. 기능 범위, 용어, 계산, 데이터, 상태 전이와 접근성은 PDD가
  우선한다. 샘플에 보이는 요소를 근거로 MVP 기능을 추가하지 않는다.
- 샘플의 실시간 뉴스/AI 분석, 간단 수익률 계산기와 PDD에 정의되지 않은 추천 포트폴리오는 MVP 구현
  대상으로 추론하지 않는다. 필요하면 비활성 모형으로도 노출하지 않는다.
- 샘플의 `10년 백테스팅`, `미상장 기간은 0원으로 계산` 같은 문구를 복사하지 않는다. 사용자 보유
  종목 화면은 PDD 용어인 `현재 보유 수량 기준 과거 추정 가치`를 사용하고 상장 전 기간을 0원으로
  계산하지 않는다.
- 샘플에 표시된 `2초 자동 갱신` 문구는 실제 적용 설정값으로 렌더링한다. 숫자 2를 UI에 하드코딩하지
  않는다.
- 샘플의 값, 티커, 날짜와 수익률은 레이아웃 예시일 뿐이다. 테스트 fixture 외에는 실제 데이터처럼
  고정하지 않는다.
- 이미지 바깥의 선명한 빨간 테두리와 가장자리의 작은 노란 표시는 캡처 표시로 간주하며 UI에 구현하지
  않는다.

### 9.2 디자인 언어

- 데스크톱 우선의 다크 금융 대시보드로 구현한다. 페이지 배경은 검정에 가까운 네이비, 카드와 입력은
  한 단계 밝은 네이비, 경계선은 저대비 slate 계열을 사용한다.
- 기본 액션과 활성 탭은 선명한 파란색을 사용한다. 경고·추천 강조는 주황/노랑, 정상·LIVE 상태는
  초록을 사용한다.
- 국내 금융 화면 관례와 샘플에 맞춰 상승은 빨강, 하락은 파랑으로 표현하되 부호, 텍스트와 아이콘을
  함께 사용한다. 장식 색과 데이터 의미 색을 혼용하지 않는다.
- 굵고 높은 대비의 수치, 작은 보조 라벨, pill 형태 상태 배지, 12~16px 내외의 카드 모서리와 촘촘한
  데이터 밀도를 일관되게 유지한다.
- 전역 헤더는 모든 주요 화면에서 동일한 높이·내비게이션·검색 구조를 유지한다. 현재 탭은 테두리와
  배경을 함께 사용해 구분한다.
- 본문은 넓은 중앙 컨테이너와 24~32px 수준의 바깥 여백을 기본으로 하며, 카드 내부 정렬선과 열 간격을
  화면 전체에서 맞춘다. 정확한 값은 구현 시 원본 이미지 픽셀 측정으로 토큰화한다.
- 표와 차트는 카드 안에서 제목, 제어 영역, 본문, 보조 설명의 순서로 구성한다. 수치 정렬에는 tabular
  numerals를 사용한다.
- 글꼴은 한글 숫자 가독성이 좋은 Pretendard를 우선 사용하고, 로딩 실패에 대비한 시스템 sans-serif
  fallback을 둔다.
- 아이콘은 한 가지 검증된 아이콘 라이브러리에서 선택한다. 이모지, 텍스트 기호, CSS 그림이나 임시
  박스로 샘플의 아이콘을 흉내 내지 않는다.

### 9.3 반응형 변환

- 샘플 해상도를 데스크톱 기준으로 재현하되 고정 폭 스크린샷을 배경 이미지로 사용하지 않는다.
  구조화된 HTML과 재사용 가능한 컴포넌트로 구현한다.
- 좁은 화면에서는 메인/보조 2열을 1열로 쌓고, 지수·추천 카드 그리드는 가용 폭에 따라 열 수를 줄인다.
- 표는 중요한 열을 임의로 삭제하지 않고 가로 스크롤을 제공한다. 상단 내비게이션은 핵심 탭을 유지하는
  스크롤 또는 명시적 메뉴 패턴으로 변환한다.
- 모바일 변환에서도 카드 계층, 데이터 의미 색, 상태 배지와 주요 CTA 순서는 유지한다.
- 차트 높이와 글자 크기에는 최소 가독성 기준을 두며 200% 확대에서 겹침·잘림이 없어야 한다.

### 9.4 컴포넌트와 토큰화

- 최초 UI 구현 전에 샘플에서 측정한 색상, 간격, radius, 그림자, 글자 크기, 컨테이너 폭을 디자인
  토큰으로 정의한다. 화면별로 임의의 유사 색상이나 간격을 새로 만들지 않는다.
- `AppHeader`, `PrimaryNav`, `StatusBadge`, `MetricCard`, `Panel`, `DataTable`, `PeriodSelector`,
  `ChartTypeToggle`, `InstrumentBadge`, `EmptyState`와 `ErrorState`를 공통 컴포넌트 후보로 사용한다.
- 공통 컴포넌트는 시각적 variant와 데이터 상태만 입력받고 공급원 호출이나 도메인 계산을 포함하지
  않는다.
- 원본 이미지 전체 또는 일부를 잘라 카드·차트·로고로 재사용하지 않는다. 차트와 텍스트는 실제 UI로
  구현하고, 제공된 브랜드 자산이 없는 경우 로고를 임의 생성하지 않는다.

### 9.5 디자인 검증 게이트

- 각 화면 구현 완료 전 원본 이미지와 동일 viewport·동일 상태의 구현 캡처를 나란히 비교한다.
- 헤더 높이, 컨테이너 폭, 카드 위치와 크기, 여백, 타이포 계층, 색상, border, radius, 차트 영역을
  눈대중이 아닌 측정값으로 검증한다.
- 시각적 차이는 기능을 막거나 구조를 바꾸는 문제(P1), 눈에 띄는 레이아웃·타이포·색상 불일치(P2),
  미세 장식 차이(P3)로 분류한다. P1과 P2를 수정한 뒤에만 화면 구현을 완료로 처리한다.
- 시각적 일치만으로 완료 처리하지 않는다. 키보드 흐름, 반응형, 200% 확대, 로딩·빈 데이터·부분 성공·
  지연·오래된 데이터·실패 상태와 사용자 입력 보존도 함께 검증한다.

## 10. UI와 상태 관리

- Server Component를 기본으로 하고 브라우저 API, 상호작용 또는 차트가 필요한 가장 작은 경계에만
  `use client`를 둔다.
- 주기 갱신은 변경된 가격·상태·차트 데이터만 갱신한다. 쿼리 키와 selector를 세분화하고 안정적인
  식별자를 사용해 입력 컴포넌트나 전체 페이지를 재마운트하지 않는다.
- 사용자의 검색어, 필터, 탭, 포커스, 스크롤, 입력 중 수량은 배경 갱신으로 초기화하지 않는다.
- 로딩, 빈 데이터, 부분 누락, 응답 지연, 오래된 데이터와 실패를 서로 다른 UI로 표현한다. 마지막 정상
  데이터에는 기준 시각과 오래된 데이터 배지를 명시한다.
- 오류 문구는 문제, 영향 범위와 다음 행동을 설명한다. 내부 예외 문자열·스택·공급원 비밀값을 그대로
  노출하지 않는다.
- 상승·하락과 상태를 색상에만 의존하지 않고 부호, 텍스트와 아이콘으로도 구분한다.
- 모든 입력은 보이는 라벨과 인접 오류 설명을 갖는다. 키보드 조작, 명확한 focus ring, 표 헤더/정렬
  속성, 차트의 표 또는 텍스트 대안을 제공한다.
- 200% 확대와 모바일 가로 스크롤을 검증한다. SVG 차트도 DOM 접근성 대체 정보를 별도로 제공한다.
- localStorage 접근은 버전이 있는 repository 어댑터를 통하고 schema 검증·migration·손상 데이터
  복구 경로를 둔다. React 컴포넌트에서 직접 읽고 쓰지 않는다.
- 최초 로컬 저장과 전체 삭제/초기화 전에 PDD의 유실 가능성과 복구 불가 안내·확인을 제공한다.

## 11. API와 오류 처리

- route handler는 인증/입력 파싱, 유스케이스 호출, DTO 변환만 담당한다. 비즈니스 계산과 공급원 호출
  조합을 route handler에 넣지 않는다.
- 공개 계약은 Zod schema와 TypeScript 타입을 함께 내보낸다. 내부 DB row나 공급원 응답을 그대로
  응답하지 않는다.
- 성공 응답에는 데이터 기준 시각, 수집 완료 시각, source, 상태와 적용 설정 중 UI가 필요한 값을 담는다.
- 오류는 안정된 `code`, 사용자용 한국어 `message`, 재시도 가능 여부와 필요한 진단 ID를 가진다.
  로그는 같은 진단 ID로 원인을 추적한다.
- 예상 가능한 부분 실패는 예외로 전체 요청을 중단하지 말고 명시적 결과 타입으로 반환한다.
- API 키, 헤더, 전체 공급원 payload, 로컬 포트폴리오 내용은 기본 로그에 남기지 않는다.

## 12. 테스트 전략과 완료 조건

변경은 위험에 비례해 아래 계층에서 검증한다. 테스트를 요구사항 구현 뒤의 선택 사항으로 취급하지 않는다.

- **도메인 단위 테스트:** PDD 계산 공식, Decimal 반올림, 공통 시작일, 상장 전 처리, 환율 forward-fill,
  MDD 고점·저점, 구성 비율 100%를 고정 fixture로 검증한다.
- **상태 머신 테스트:** fake clock으로 2초 주기/5초 타임아웃의 0·6·12·18초 시작과 23초 그룹 중단,
  회차 건너뛰기, 성공 시 초기화, 그룹별 복구 및 장 재개 원점을 검증한다.
- **계약 테스트:** Yahoo fixture에 대한 schema/mapping, 잘못된 OHLCV 거부, UTC 변환, 배치 부분 성공,
  마지막 정상 스냅샷 교체 규칙을 검증한다. 실 네트워크 응답에 단위 테스트를 의존시키지 않는다.
- **컴포넌트 테스트:** 모든 데이터 상태, 한도 초과, 설정 축소 시 기존 데이터 보존, 입력 중 배경 갱신과
  키보드 흐름을 검증한다.
- **E2E:** PDD의 시장 탐색, 종목 분석, 포트폴리오 분석, 추천안 복사 및 실패 복구 핵심 흐름을 검증한다.
- **부하·스모크:** 적용 자동 조회 주기로 네 그룹을 1분간 실행해 그룹별 요청 수, 평균/p95 응답 시간,
  응답 지연과 건너뛰기 횟수를 기록한다. 지연과 건너뛰기가 모두 0이어야 합격이다.
- **안정성:** 최소 1시간 또는 한 시장 세션 동안 메모리, 그룹별 오류율, HTTP 429, 연결 재수립과
  처리되지 않은 예외를 기록한다. 이 장시간 테스트는 명시적 릴리스/수용 검증에서 수행한다.

일반 변경의 최소 완료 조건:

1. `pnpm lint`, `pnpm typecheck`, `pnpm test`가 통과한다.
2. 사용자 흐름 또는 API 계약 변경은 관련 Playwright 테스트를 통과한다.
3. DB schema 변경은 되돌릴 수 있는 migration과 repository 테스트를 포함한다.
4. 설정, 계산 가정 또는 사용자 동작이 바뀌면 PDD/ADR/운영 문서 중 해당 문서를 함께 갱신한다.
5. 실패한 검증이 있으면 결과를 숨기지 않고 원인, 영향과 미실행 항목을 작업 보고에 명시한다.

## 13. 코딩·변경 규칙

- TypeScript strict를 유지한다. `any`, non-null assertion과 무분별한 type assertion 대신 경계 검증과
  좁히기를 사용한다.
- 함수와 변수 이름은 영어로, 사용자 표시 문구는 한국어로 작성한다. 도메인 용어는 PDD 용어와 맞춘다.
- 명백한 코드를 선호하고 추상화는 두 개 이상의 실제 구현 또는 테스트 경계가 필요할 때 도입한다.
- 외부 I/O는 포트 뒤에 두고 순수 계산과 상태 전이를 작은 함수로 분리한다.
- 매직 넘버, 기간 목록, 상태 문자열, 조회 주기와 한도는 중앙 정책/설정에서 가져온다.
- 사용자 저장 데이터를 삭제·덮어쓰는 동작은 변경 전후 미리보기, 명시적 확인과 취소 시 무변경을
  보장한다.
- 의존성 추가는 번들 크기, 유지 상태, 라이선스, 접근성과 기존 스택 중복을 검토한다. 단순 유틸리티를
  위해 큰 런타임 의존성을 추가하지 않는다.
- 공급원 제약을 발견하면 가짜 값이나 다른 티커로 조용히 대체하지 않는다. 특히 All Weather 원자재는
  `DBC`를 유지하고 공급원 심볼 매핑만 검증한다.
- 기능 변경은 가능한 작고 수직적인 단위로 수행한다. 관련 없는 리팩터링이나 사용자 변경을 덮어쓰지
  않는다.
- 새로운 구조적 결정은 `docs/adr/NNNN-<slug>.md`에 문제, 결정, 대안, 결과를 간결히 기록한다.

## 14. 문서 동기화

- PDD의 버전·상태와 실제 구현 차이를 발견하면 `memory-bank/progress.md`에 구현 상태와 차이를 기록한다.
- 구현 순서나 마일스톤이 정해지면 `memory-bank/implementation-plan.md`를 갱신한다.
- 공급원 범위, 이용 조건, 지연, 요청 한도, 데이터 간격과 심볼 매핑의 검증 결과는 재현 가능한 날짜와
  방법을 포함해 문서화한다.
- 환경별 적용 설정과 변경 이유, 부하·스모크·안정성 결과를 기록하되 비밀값은 포함하지 않는다.
- README에는 설치, 설정, DB migration, 개발 서버, 품질 검사와 테스트 명령을 실제 스크립트와 동일하게
  유지한다.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
